[workspace]
resolver = "2"
members = [
  "crates/dotrc-core",
  "crates/dotrc-core-wasm",
  "crates/dotrc-server"
]

# Optimize WASM binary for size (only opt-level can be per-package)
[profile.release.package.dotrc-core-wasm]
opt-level = "z"
strip = true
