name = "dotrc-worker"
main = "src/index.ts"
compatibility_date = "2025-12-20"

# Bindings
# TODO: create the D1 database and fill in values below
# [[d1_databases]]
# binding = "DB"
# database_name = "dotrc"
# database_id = "REPLACE_WITH_D1_ID"

# WASM is imported directly in ESM via src/index.ts; no bindings needed
